<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.usb.host" android:required="true" />
    <uses-permission android:name="android.permission.USB_PERMISSION" />

    <application
        android:allowBackup="true"
        android:largeHeap="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ant"
        android:label="TKey Loader"
        android:roundIcon="@drawable/ant"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tkey"
        tools:targetApi="31">

        <activity
            android:name="com.tillitis.tkey.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:directBootAware="true"
            android:label="TKey Loader"
            android:theme="@style/Theme.Tkey">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="com.tillitis.tkey.USB_PERMISSION" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>

            <meta-data android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>
        <service
            android:name="com.tillitis.tkey.client.UsbService"
            android:enabled="true">
        </service>

    </application>

</manifest>